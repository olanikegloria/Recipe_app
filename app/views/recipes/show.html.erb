<div class="container">
     
     <h2 class="mb-4"><%= @recipe.name %></h2>

        <p><%= @recipe.description %></p>

        <h5>Preparation Time: <b><%= @recipe.preparation_time %></b></h5>
        <h5>Cooking Time: <b><%= @recipe.cooking_time %></b></h5>


        <div class="d-flex justify-content-between align-items-center border-bottom border-top py-2 mt-3">
            <a  href="#" class="btn btn-primary">Generate shopping list</a>
            <a  href="#" class="btn btn-primary">Add Ingredients</a>
        </div>

        <table class="table table-bordered table-striped table-hover">
            <thead>
                <th>Food</th>
                <th>Quantity</th>
                <th>Value</th>
                <th>Actions</th>
            </thead>
        </table>

     </div>