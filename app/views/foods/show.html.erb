<div class="container">
     
     <h2 class="mb-4"><%= @food.name %></h2>

        <p><%= @food.quantity %></p>

        <h5>Measurement unit : <b><%= @food.measurement_unit %></b></h5>
        <h5>Price : <b><%= @food.price %></b></h5>


        <div class="d-flex justify-content-between align-items-center border-bottom border-top py-2 mt-3">
            <a  href="#" class="btn btn-primary">Generate shopping list</a>
            <a  href="#" class="btn btn-primary">Add Ingredients</a>
        </div>

        <table class="table table-bordered table-striped table-hover">
            <thead>
                <th>Food</th>
                <th>Quantity</th>
                <th>Value</th>
                <th>Actions</th>
            </thead>
        </table>

     </div>